---
name: cxp-knowledge-video
description: 将中文文章、完整文案或录音制作成纸张手绘知识图解视频；按内容自适应分章，每章一张原片风格图解，文章轻改口播并自动配音，录音保留原声，配语义同步局部强调，输出无烧录字幕的竖屏 MP4。适用于知识图解视频、文章或录音做成原片风格视频，不替代 PPT 转视频或逐笔白板绘制技能。
---

# cxp-知识图解视频

将用户素材做成可播放成片，完成图片、音频、动画与质检后交付。默认 1080×1920、9:16、30fps。视觉锚点是附带的《职场沟通结论先行法则》参考帧；**章节数量由本次内容决定，每章恰好一张独立完整图解**。

## 用户选择

- **不烧录旁白字幕，也不嵌入软字幕。** 标题、关键词、流程标签是图解内容，应保留；不把无字幕误解为无文字。为用户后期加字幕留安全带，不显示字幕底框、占位文字或时间码。不默认生成 SRT，内部时间码 JSON 用于动画对齐。
- 输入只有录音：本地转写、校正术语、按录音内容分章；使用原录音，不重写后替换声音，不擅自加速/截短。视频输入提取声音，已有画面仅在用户指定时作参考。
- 输入文章/完整文案：允许去赘词、拆长句、增加自然衔接，保留观点、论证顺序、案例、事实与条件，轻改口播并自动配音。无需再要求用户自己录音；本技能明确允许 TTS，不套用其他技能的只能本人录音规定。
- 同时给录音和文字，默认录音是成片声音，文字用于纠错。遇到影响事实或含义的冲突，先说明并询问，不静默改原声。
- 动画是**主图稳定 + 随语义依次强调 + 短叠化转场**。同章不换构图、不局部放大跳动、不整页翻转露黑、不假装逐笔手写。
- 自动推进，仅素材缺失、实质内容冲突或外部能力不可用时报告具体阻碍。局部失败最多针对性修复两次后说明；不能把提示词、样片或无声片冒充完成视频。

## 执行

1. **读素材与定声源。** 保存原文或音频路径/哈希，写 production.json 的 inputMode。文章模式保存 narration.txt 与简短修改记录；录音模式保留原时间，不用未校正 ASR 当正文。读 [voice.md](references/voice.md)。
2. **语义分章。** 读 [planning.md](references/planning.md)。一章回答一个问题或呈现一个完整关系，不能平均按字数切分，不固定五章。写 storyboard.json：每章原文对应范围、口播、目标、标题、准确短标签、布局、隐喻。章节与最终独立图像一一对应，不同章不复用同一张主图。
3. **音频与时间。** 文章按章合成，用实测时长拼接；保存旁白与配音清单。录音在自然语义边界标时间，保留连续原声。章节连续覆盖声音，强调点从实际语音/词时间码校准，不均分。
4. **生成每章图。** 读 [visual-style.md](references/visual-style.md)，用 view_image 检查附带参考图。通过内置图像生成工具逐章生成新图；参考的是美术语言与构图方法，**不复制截图中的旁白字幕、VibeKnow 水印、固定话题或编号错误**。本轮第一张合格正文图作为后续风格参照。逐图检查中文、布局、箭头和字幕安全带，不以通用代码卡片替代手绘插图。
5. **编排强调。** 在最终图片真实区域标框，写 production.json 的 cues。每条完整解释对应一次柔和描边/圈选，进入约 0.2s，结束约 0.25s 消退；同一时刻最多一个主要强调。读长例子可保持，不为运动机械闪烁。转场约 0.35s，在章节边界中心叠化，声音不中断。
6. **渲染。** `scripts/render_video.py production.json --out 视频.mp4`，依赖 Python、Pillow、FFmpeg/ffprobe。见 [production-format.md](references/production-format.md)。渲染器不接收字幕文本；图片本身的字幕须在视觉检查阶段拦截。中间文件限本次项目，不清理用户目录。
7. **验证交付。** `scripts/verify_video.py production.json 视频.mp4 --out quality-report.json` 检查规格、时长、解码与音量，并提取代表帧。检查首帧、各章图、强调前中后、转场与结尾；写 review.json 记录图片文案对应、原片风格、无口播字幕、留白、可读性、箭头、音画同步及修复。技术通过不等于语义/美术通过；二者通过才报告成片完成。

## 交付范围

默认输出 `D:\codex办公产物\日期\主题`（跨机器使用用户工作目录）：无字幕 MP4、无字幕章节图、最终旁白、口播稿、分镜时间线、质检记录。默认不生成发布文案、平台封面、额外比例。优先展示成片及文件链接，简述时长/章数/声音模式，不用项目代码代替视频。

图片无可用工具时保存文稿分镜并说明，不私自切收费 API。配音要实际联网验证，不能把装库当成功。云端只发送需要朗读的文本，不上传参考音频克隆；新敏感数据传输和付费后端按本次授权处理。本技能独立运行，不引入其他技能的字幕、固定章节、TTS 禁止、封面或静态画面禁止规则。
